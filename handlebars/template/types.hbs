{{!-- types.hbs --}}
import { z } from "zod";
import {
{{#each schemas}}
  {{@key}}Schema,
{{/each}}
} from "./schema.zod.js";

{{#each schemas}}
type {{@key}} = z.infer<typeof {{@key}}Schema>;
{{/each}}

export type { {{#each schemas}}{{@key}}{{#unless @last}}, {{/unless}}{{/each}} };
